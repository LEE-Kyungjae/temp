// lib/views/crew_view.dart
import 'package:flutter/material.dart';

class CrewView extends StatelessWidget {
  const CrewView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('크루 페이지'),
      ),
    );
  }
}
